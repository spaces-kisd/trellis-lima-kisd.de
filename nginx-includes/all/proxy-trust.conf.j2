# nginx-includes/proxy-trust.conf.j2

# Trust the IP of the upstream proxy
set_real_ip_from 139.6.141.9;

# Use the X-Forwarded-For header to determine the real client IP
real_ip_header X-Forwarded-For;

# Recursive allows Nginx to dig through multiple proxies if necessary
real_ip_recursive on;
